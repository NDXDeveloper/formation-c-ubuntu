🔝 Retour au [Sommaire](/SOMMAIRE.md)

# 5.7.3 Intégration IDE

## Introduction

L'un des grands avantages de clang-format est sa capacité à s'intégrer dans pratiquement tous les éditeurs de texte et IDE modernes. Au lieu de formater manuellement votre code ou d'exécuter des commandes en ligne de commande, vous pouvez configurer votre éditeur pour :

- **Formater automatiquement à la sauvegarde** du fichier
- **Formater sur commande** avec un raccourci clavier
- **Afficher les erreurs de formatage** en temps réel
- **Formater uniquement les lignes modifiées**

Cette section vous guidera à travers l'intégration de clang-format dans les éditeurs et IDE les plus populaires.

---

## Visual Studio Code (VS Code)

### Pourquoi VS Code ?

VS Code est l'un des éditeurs les plus populaires pour le développement C/C++ sur Linux. Il est :
- ✅ Gratuit et open-source
- ✅ Léger et rapide
- ✅ Doté d'excellentes extensions
- ✅ Multi-plateforme

### Installation de l'extension

**Méthode 1 : Via l'interface graphique**

1. Ouvrir VS Code
2. Cliquer sur l'icône Extensions dans la barre latérale (ou `Ctrl+Shift+X`)
3. Rechercher "C/C++"
4. Installer l'extension officielle **"C/C++" par Microsoft**

**Méthode 2 : Via la ligne de commande**

```bash
code --install-extension ms-vscode.cpptools
```

**⚠️ Note :** L'extension C/C++ inclut le support de clang-format. Il n'y a pas besoin d'installer une extension séparée.

### Vérifier que clang-format est disponible

VS Code utilise clang-format s'il est installé sur votre système :

```bash
# Vérifier l'installation
which clang-format
clang-format --version
```

Si clang-format n'est pas installé, suivez les instructions de la section 5.7.1.

### Configuration de base

#### Ouvrir les paramètres

1. `Ctrl+,` ou `Fichier > Préférences > Paramètres`
2. Rechercher "format"

#### Configuration JSON

Vous pouvez aussi éditer directement le fichier `settings.json` :

1. `Ctrl+Shift+P` pour ouvrir la palette de commandes
2. Taper "Preferences: Open Settings (JSON)"
3. Ajouter la configuration

**Configuration recommandée pour C :**

```json
{
    // === ÉDITEUR GÉNÉRAL ===
    "editor.formatOnSave": true,
    "editor.formatOnType": false,
    "editor.formatOnPaste": false,

    // === C/C++ SPÉCIFIQUE ===
    "[c]": {
        "editor.defaultFormatter": "ms-vscode.cpptools",
        "editor.formatOnSave": true
    },

    "[cpp]": {
        "editor.defaultFormatter": "ms-vscode.cpptools",
        "editor.formatOnSave": true
    },

    // === CLANG-FORMAT ===
    "C_Cpp.clang_format_path": "/usr/bin/clang-format",
    "C_Cpp.clang_format_style": "file",
    "C_Cpp.clang_format_fallbackStyle": "LLVM",

    // === AUTRES PARAMÈTRES UTILES ===
    "editor.rulers": [80, 100],
    "files.insertFinalNewline": true,
    "files.trimTrailingWhitespace": true,
    "editor.tabSize": 4,
    "editor.insertSpaces": true
}
```

**Explications :**
- `editor.formatOnSave` : Formate automatiquement à chaque sauvegarde
- `C_Cpp.clang_format_style: "file"` : Utilise le fichier `.clang-format` du projet
- `C_Cpp.clang_format_fallbackStyle: "LLVM"` : Style par défaut si pas de `.clang-format`
- `editor.rulers` : Affiche des guides verticaux à 80 et 100 caractères

### Utilisation dans VS Code

#### Formater manuellement

**Raccourcis clavier :**
- `Shift+Alt+F` : Formater tout le document
- `Ctrl+K Ctrl+F` : Formater la sélection

**Via la palette de commandes :**
1. `Ctrl+Shift+P`
2. Taper "Format Document" ou "Format Selection"

#### Formater automatiquement

Si vous avez activé `"editor.formatOnSave": true`, le code sera formaté automatiquement à chaque sauvegarde (`Ctrl+S`).

#### Formater uniquement les lignes modifiées

```json
{
    "C_Cpp.clang_format_sortIncludes": true,
    "editor.formatOnSaveMode": "modifications"
}
```

Cette option ne formate que les lignes que vous avez modifiées, utile dans les gros projets existants.

### Configuration par projet (Workspace)

Vous pouvez avoir des configurations différentes par projet :

1. Ouvrir un dossier dans VS Code
2. Créer `.vscode/settings.json` dans le dossier du projet
3. Ajouter vos paramètres spécifiques

**Exemple `.vscode/settings.json` :**

```json
{
    "C_Cpp.clang_format_style": "file",
    "editor.formatOnSave": true,
    "editor.rulers": [80]
}
```

Ces paramètres **surchargent** les paramètres utilisateur globaux.

### Désactiver temporairement le formatage

Si vous voulez sauvegarder sans formater :

1. `Ctrl+K S` : "Save without Formatting"

Ou temporairement dans `settings.json` :
```json
{
    "editor.formatOnSave": false
}
```

### Extensions complémentaires utiles

**clangd** (alternative à l'extension C/C++ officielle) :

```bash
# Installer l'extension clangd
code --install-extension llvm-vs-code-extensions.vscode-clangd
```

clangd offre :
- Autocomplétion plus rapide
- Meilleure intégration avec clang-format
- Diagnostics en temps réel

**Configuration avec clangd :**

```json
{
    "clangd.arguments": [
        "--clang-tidy",
        "--background-index",
        "--header-insertion=iwyu",
        "--completion-style=detailed"
    ],
    "[c]": {
        "editor.defaultFormatter": "llvm-vs-code-extensions.vscode-clangd"
    }
}
```

---

## Vim / Neovim

### Pourquoi Vim ?

Vim est l'éditeur préféré de nombreux développeurs système et Linux pour sa :
- ✅ Puissance et efficacité
- ✅ Disponibilité sur tous les systèmes Unix
- ✅ Personnalisation infinie
- ✅ Vitesse d'édition avec clavier uniquement

### Installation du plugin

**Méthode 1 : vim-clang-format (plugin dédié)**

Avec **vim-plug** (gestionnaire de plugins populaire) :

1. Installer vim-plug si nécessaire :
```bash
curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
```

2. Ajouter à votre `~/.vimrc` :
```vim
call plug#begin('~/.vim/plugged')

" Plugin clang-format
Plug 'rhysd/vim-clang-format'

call plug#end()
```

3. Installer le plugin :
```vim
:PlugInstall
```

**Méthode 2 : Utilisation native (sans plugin)**

Vim peut utiliser clang-format directement via les outils externes.

### Configuration dans .vimrc

**Configuration complète pour vim-clang-format :**

```vim
" === PLUGIN CLANG-FORMAT ===
let g:clang_format#command = 'clang-format'
let g:clang_format#detect_style_file = 1
let g:clang_format#auto_format = 1

" Formater automatiquement à la sauvegarde (optionnel)
let g:clang_format#auto_format_on_insert_leave = 0

" Raccourci pour formater : <Leader>cf
autocmd FileType c,cpp nnoremap <buffer><Leader>cf :<C-u>ClangFormat<CR>
autocmd FileType c,cpp vnoremap <buffer><Leader>cf :ClangFormat<CR>

" Formater automatiquement à la sauvegarde pour C/C++
autocmd FileType c,cpp autocmd BufWritePre <buffer> :ClangFormat
```

**Configuration native (sans plugin) :**

```vim
" Formater avec clang-format via une commande externe
autocmd FileType c,cpp setlocal formatprg=clang-format

" Raccourci pour formater le fichier entier
autocmd FileType c,cpp nnoremap <buffer><Leader>cf :% !clang-format<CR>

" Raccourci pour formater une sélection visuelle
autocmd FileType c,cpp vnoremap <buffer><Leader>cf :!clang-format<CR>
```

**Explication des options :**
- `<Leader>` : Touche leader (par défaut `\`)
- `<Leader>cf` : Raccourci pour formater (par exemple `\cf`)
- `BufWritePre` : Événement déclenché avant sauvegarde

### Utilisation dans Vim

**Avec le plugin vim-clang-format :**

```vim
" En mode normal : formater tout le fichier
:ClangFormat

" En mode visuel : sélectionner du code puis
:ClangFormat

" Avec le raccourci (si configuré)
\cf     " (Leader + cf)
```

**Avec la configuration native :**

```vim
" Formater tout le fichier
gggqG

" Ou avec le raccourci
\cf

" En mode visuel : sélectionner puis
gq
```

### Neovim et configuration Lua

Si vous utilisez Neovim avec une configuration Lua moderne :

**`~/.config/nvim/init.lua` :**

```lua
-- Installation avec packer.nvim
require('packer').startup(function(use)
  use 'rhysd/vim-clang-format'
end)

-- Configuration
vim.g['clang_format#command'] = 'clang-format'
vim.g['clang_format#detect_style_file'] = 1
vim.g['clang_format#auto_format'] = 0

-- Keybindings
vim.api.nvim_set_keymap('n', '<Leader>cf', ':ClangFormat<CR>', { noremap = true, silent = true })
vim.api.nvim_set_keymap('v', '<Leader>cf', ':ClangFormat<CR>', { noremap = true, silent = true })

-- Auto-format à la sauvegarde
vim.api.nvim_create_autocmd("BufWritePre", {
  pattern = { "*.c", "*.cpp", "*.h", "*.hpp" },
  callback = function()
    vim.cmd('ClangFormat')
  end,
})
```

### Vérifier le chemin de clang-format

Dans Vim, vérifiez que clang-format est accessible :

```vim
:!which clang-format
:!clang-format --version
```

---

## Emacs

### Pourquoi Emacs ?

Emacs est un éditeur puissant et extensible, très populaire dans la communauté GNU/Linux.

### Installation et configuration

**1. Vérifier que clang-format est installé :**

```bash
which clang-format
```

**2. Configuration dans `~/.emacs` ou `~/.emacs.d/init.el` :**

```elisp
;; === CLANG-FORMAT ===

;; Charger clang-format
(load "/usr/share/clang/clang-format.el")

;; Fonction pour formater le buffer ou la région
(defun my-clang-format-buffer ()
  "Format the current buffer with clang-format."
  (interactive)
  (if (use-region-p)
      (clang-format-region (region-beginning) (region-end))
    (clang-format-buffer)))

;; Raccourci clavier : C-c C-f
(global-set-key (kbd "C-c C-f") 'my-clang-format-buffer)

;; Auto-format à la sauvegarde pour C/C++
(add-hook 'c-mode-hook
          (lambda ()
            (add-hook 'before-save-hook 'clang-format-buffer nil 'local)))

(add-hook 'c++-mode-hook
          (lambda ()
            (add-hook 'before-save-hook 'clang-format-buffer nil 'local)))

;; Style clang-format (utilise .clang-format si présent)
(setq clang-format-style "file")
```

**3. Trouver le chemin de clang-format.el :**

```bash
# Chercher le fichier
find /usr -name "clang-format.el" 2>/dev/null
```

Chemin typique : `/usr/share/clang/clang-format.el` ou `/usr/share/emacs/site-lisp/clang-format.el`

### Utilisation dans Emacs

**Formater manuellement :**
- `C-c C-f` : Formater le buffer ou la sélection (si configuré comme ci-dessus)

**Formater automatiquement :**
- Activé avec le hook `before-save-hook` dans la configuration ci-dessus

### Alternative : use-package

Si vous utilisez `use-package` :

```elisp
(use-package clang-format
  :ensure t
  :bind (("C-c C-f" . clang-format-buffer))
  :hook ((c-mode . (lambda ()
                     (add-hook 'before-save-hook 'clang-format-buffer nil t)))
         (c++-mode . (lambda ()
                       (add-hook 'before-save-hook 'clang-format-buffer nil t)))))
```

---

## CLion (JetBrains)

### Pourquoi CLion ?

CLion est un IDE commercial puissant de JetBrains, offrant :
- ✅ Excellent support C/C++
- ✅ Refactoring avancé
- ✅ Débogueur intégré
- ✅ Intégration CMake native

### Configuration

CLion a un support natif de clang-format.

**1. Activer clang-format :**

1. Ouvrir `File > Settings` (ou `Ctrl+Alt+S`)
2. Naviguer vers `Editor > Code Style > C/C++`
3. Cocher **"Enable ClangFormat (only for C/C++/Objective-C)"**

**2. Configurer le chemin :**

Dans la même fenêtre :
- **ClangFormat binary** : `/usr/bin/clang-format` (ou le chemin de votre installation)

**3. Utiliser le fichier .clang-format :**

- Cocher **"Use .clang-format file"**

CLion cherchera automatiquement le fichier `.clang-format` dans votre projet.

**4. Formater automatiquement :**

1. `File > Settings > Tools > Actions on Save`
2. Cocher **"Reformat code"**

### Utilisation dans CLion

**Formater manuellement :**
- `Ctrl+Alt+L` : Formater le fichier ou la sélection
- Clic droit > `Reformat Code`

**Formater automatiquement :**
- Activé avec "Actions on Save" (voir ci-dessus)

### Désactiver temporairement

Pour désactiver le formatage sur une section :

```c
// @formatter:off
int tableau[3][3] = {
    {1, 2, 3},
    {4, 5, 6},
    {7, 8, 9}
};
// @formatter:on
```

---

## Qt Creator

### Pourquoi Qt Creator ?

Qt Creator est un IDE léger et rapide, pas seulement pour Qt :
- ✅ Excellent pour C/C++ pur
- ✅ Support CMake natif
- ✅ Gratuit et open-source

### Configuration

**1. Activer Beautifier (plugin intégré) :**

1. `Tools > Options > Beautifier`
2. Onglet **"Clang Format"**
3. Cocher **"Enable Clang Format"**

**2. Configurer le chemin :**

- **Clang Format command** : `/usr/bin/clang-format`

**3. Options de formatage :**

- **Use predefined style** : "File" (pour utiliser `.clang-format`)
- Ou choisir un style prédéfini : "LLVM", "Google", etc.

**4. Formater automatiquement :**

Dans `Tools > Options > Beautifier > General` :
- Cocher **"Enable auto format on file save"**

### Utilisation dans Qt Creator

**Formater manuellement :**
- `Ctrl+Alt+F` : Formater le fichier
- `Tools > Beautifier > ClangFormat > Format Current File`

**Formater automatiquement :**
- Activé avec "Enable auto format on file save"

---

## Sublime Text

### Configuration

Sublime Text nécessite un plugin pour clang-format.

**1. Installer Package Control :**

1. Ouvrir la console : `Ctrl+` ` (backtick)
2. Copier-coller le code d'installation depuis https://packagecontrol.io/installation

**2. Installer le plugin Clang Format :**

1. `Ctrl+Shift+P` : Ouvrir la palette de commandes
2. Taper "Package Control: Install Package"
3. Chercher et installer **"Clang Format"**

**3. Configuration :**

`Preferences > Package Settings > Clang Format > Settings - User`

```json
{
    "binary": "/usr/bin/clang-format",
    "style": "file",
    "format_on_save": true
}
```

### Utilisation

**Formater manuellement :**
- `Ctrl+Alt+A` : Formater tout le fichier
- Ou via la palette de commandes : "Clang Format: Format Entire File"

---

## Kate (KDE)

### Configuration

Kate, l'éditeur de KDE, a un support natif de clang-format.

**1. Activer le plugin :**

1. `Settings > Configure Kate > Plugins`
2. Cocher **"Format"**

**2. Configuration :**

1. `Settings > Configure Kate > Format`
2. **Formatter** : Sélectionner "ClangFormat"
3. **Style** : "File" (pour utiliser `.clang-format`)
4. Cocher **"Format on Save"**

### Utilisation

**Formater manuellement :**
- `Tools > Format`
- Ou raccourci clavier (configurable)

---

## Comparaison des éditeurs

| Éditeur | Installation | Facilité | Auto-format | Popularité C |
|---------|--------------|----------|-------------|--------------|
| **VS Code** | Extension | ⭐⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐⭐ |
| **Vim** | Plugin | ⭐⭐⭐ | ✅ | ⭐⭐⭐⭐⭐ |
| **Neovim** | Plugin | ⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐ |
| **Emacs** | Script elisp | ⭐⭐ | ✅ | ⭐⭐⭐⭐ |
| **CLion** | Natif | ⭐⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐⭐ |
| **Qt Creator** | Natif | ⭐⭐⭐⭐ | ✅ | ⭐⭐⭐⭐ |
| **Sublime Text** | Plugin | ⭐⭐⭐⭐ | ✅ | ⭐⭐⭐ |
| **Kate** | Natif | ⭐⭐⭐⭐ | ✅ | ⭐⭐⭐ |

**Recommandations :**
- **Débutant** : VS Code (facile et complet)
- **Développeur expérimenté** : Vim/Neovim (efficacité)
- **IDE complet** : CLion (features avancées, payant)
- **Léger et rapide** : Kate, Sublime Text

---

## Configuration multi-éditeur

Si vous travaillez avec plusieurs éditeurs ou en équipe, utilisez `.editorconfig` en complément :

**`.editorconfig` à la racine du projet :**

```ini
# EditorConfig : https://editorconfig.org

root = true

# Tous les fichiers
[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

# Fichiers C/C++
[*.{c,h,cpp,hpp}]
indent_style = space
indent_size = 4
max_line_length = 100

# Makefiles (nécessitent des tabs)
[Makefile]
indent_style = tab
```

`.editorconfig` est supporté nativement par la plupart des éditeurs modernes et assure une configuration de base cohérente.

---

## Problèmes courants et solutions

### Problème : clang-format ne fonctionne pas

**Symptômes :**
- Rien ne se passe lors du formatage
- Erreur "clang-format not found"

**Solutions :**

1. **Vérifier que clang-format est installé :**
```bash
which clang-format
clang-format --version
```

2. **Vérifier le chemin dans l'éditeur :**
   - VS Code : `C_Cpp.clang_format_path`
   - Vim : `let g:clang_format#command`
   - Etc.

3. **Ajouter clang-format au PATH :**
```bash
export PATH=$PATH:/usr/bin
```

### Problème : Formatage incorrect

**Symptômes :**
- Le code est formaté mais pas comme attendu
- Style différent de celui configuré

**Solutions :**

1. **Vérifier que le fichier .clang-format existe :**
```bash
ls -la .clang-format
```

2. **Vérifier le style utilisé :**
```bash
clang-format -style=file -dump-config mon_fichier.c | head -20
```

3. **Forcer l'utilisation du fichier .clang-format :**
   - VS Code : `"C_Cpp.clang_format_style": "file"`
   - Ligne de commande : `clang-format -style=file`

### Problème : Formatage trop lent

**Symptômes :**
- Délai perceptible lors de la sauvegarde
- Éditeur qui rame

**Solutions :**

1. **Désactiver le formatage automatique pour les gros fichiers :**

VS Code :
```json
{
    "C_Cpp.formatting": "Disabled"  // Désactiver temporairement
}
```

2. **Formater seulement les lignes modifiées :**

VS Code :
```json
{
    "editor.formatOnSaveMode": "modifications"
}
```

3. **Utiliser clangd (plus rapide) :**
```bash
sudo apt install clangd
```

### Problème : Conflit entre éditeurs

**Symptômes :**
- Le code est reformaté différemment selon l'éditeur
- Conflits Git constants

**Solution :**

1. **S'assurer que tous les éditeurs utilisent le même .clang-format :**
   - Commiter `.clang-format` dans Git
   - Documenter dans le README

2. **Vérifier les configurations locales :**
   - VS Code : Pas de settings.json avec style hardcodé
   - Vim : Pas de style alternatif dans .vimrc

3. **Tester le formatage en ligne de commande :**
```bash
clang-format -style=file mon_fichier.c
```

---

## Script de vérification de configuration

Créez un script `check_format_setup.sh` pour vérifier votre configuration :

```bash
#!/bin/bash
# Vérifier la configuration clang-format

echo "=== Vérification de l'installation clang-format ==="

# Vérifier clang-format
if command -v clang-format &> /dev/null; then
    echo "✅ clang-format est installé"
    clang-format --version
else
    echo "❌ clang-format n'est pas installé"
    echo "   Installer avec : sudo apt install clang-format"
    exit 1
fi

# Vérifier .clang-format
if [ -f ".clang-format" ]; then
    echo "✅ Fichier .clang-format trouvé"

    # Tester le formatage
    echo "   Test de formatage..."
    echo '#include <stdio.h>
int main(){return 0;}' | clang-format > /dev/null 2>&1

    if [ $? -eq 0 ]; then
        echo "✅ Configuration valide"
    else
        echo "❌ Erreur dans la configuration"
    fi
else
    echo "⚠️  Pas de fichier .clang-format"
    echo "   Le style par défaut sera utilisé"
fi

# Vérifier VS Code
if command -v code &> /dev/null; then
    echo "✅ VS Code est installé"

    # Vérifier l'extension C++
    if code --list-extensions | grep -q "ms-vscode.cpptools"; then
        echo "✅ Extension C/C++ installée"
    else
        echo "⚠️  Extension C/C++ non installée"
        echo "   Installer avec : code --install-extension ms-vscode.cpptools"
    fi
fi

# Vérifier Vim
if command -v vim &> /dev/null; then
    echo "✅ Vim est installé"

    if [ -f "$HOME/.vimrc" ]; then
        if grep -q "clang-format" "$HOME/.vimrc"; then
            echo "✅ Configuration Vim trouvée"
        else
            echo "⚠️  Pas de configuration clang-format dans .vimrc"
        fi
    fi
fi

echo ""
echo "=== Vérification terminée ==="
```

**Utilisation :**
```bash
chmod +x check_format_setup.sh
./check_format_setup.sh
```

---

## Workflow recommandé

### Pour un nouveau projet

1. **Créer le fichier .clang-format** à la racine
2. **Commiter dans Git** : `git add .clang-format`
3. **Configurer votre éditeur** pour utiliser ce fichier
4. **Activer le formatage automatique** à la sauvegarde
5. **Documenter** dans le README

### Pour un projet existant

1. **Créer .clang-format** avec le style actuel
2. **Tester** sur quelques fichiers
3. **Formater tout le projet** en un commit dédié :
```bash
find . -name "*.c" -o -name "*.h" | xargs clang-format -i
git commit -m "style: Apply clang-format to entire codebase"
```
4. **Configurer les éditeurs** de l'équipe
5. **Ajouter un pre-commit hook** (voir section 5.7.4)

### En équipe

1. **Convention** : Tous utilisent le même `.clang-format`
2. **Documentation** : Expliquer le setup dans le README
3. **CI/CD** : Vérifier le formatage automatiquement (voir section 5.7.5)
4. **Revue de code** : Ne plus commenter le style, se concentrer sur la logique

---

## Résumé

### Configuration minimale recommandée

**Pour VS Code (débutant) :**
```json
{
    "editor.formatOnSave": true,
    "[c]": {
        "editor.defaultFormatter": "ms-vscode.cpptools"
    },
    "C_Cpp.clang_format_style": "file"
}
```

**Pour Vim (expérimenté) :**
```vim
Plug 'rhysd/vim-clang-format'
let g:clang_format#detect_style_file = 1
autocmd FileType c,cpp autocmd BufWritePre <buffer> :ClangFormat
```

### Points clés

- ✅ Tous les éditeurs modernes supportent clang-format
- ✅ Configuration centralisée dans `.clang-format`
- ✅ Formatage automatique à la sauvegarde recommandé
- ✅ Documenter le setup pour l'équipe
- ✅ Utiliser `.editorconfig` en complément

### Avantages de l'intégration IDE

- 🚀 Gain de temps énorme
- 🎯 Code toujours formaté correctement
- 🤝 Cohérence dans toute l'équipe
- 💡 Focus sur la logique, pas le style
- ✨ Code professionnel instantanément

---

## Pour aller plus loin

Dans les prochaines sections, vous découvrirez :
- **5.7.4 Pre-commit hooks Git** : Automatiser le formatage avant les commits
- **5.7.5 Formatage dans le CI/CD** : Vérification automatique dans GitHub Actions

L'intégration de clang-format dans votre éditeur est un investissement de quelques minutes qui vous fera gagner des heures. Prenez le temps de bien configurer votre environnement dès maintenant !

⏭️ [Pre-commit hooks Git](/05-operateurs-et-controle/07.4-pre-commit-hooks.md)
